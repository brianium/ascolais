(ns {{top/ns}}.{{main}}-test
  (:require [clojure.test :refer [deftest is testing]]
            [{{top/ns}}.{{main}} :as {{main}}]))

(deftest example-test
  (testing "basic assertion"
    (is (= 1 1))))
