# Database Configuration
# These default to the values in config.clj if not set
JDBC_URL=jdbc:postgresql://localhost:5432/{{main}}_dev
DB_USERNAME=postgres
DB_PASSWORD=postgres

# Google OAuth Configuration
# Create credentials at https://console.cloud.google.com
# Set redirect URI to: http://localhost:3000/auth/callback (dev)
#                  or: https://your-domain.com/auth/callback (prod)
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# Session Encryption Key
# Must be exactly 16 characters for AES encryption
# Generate with: openssl rand -base64 12 | head -c 16
SESSION_SECRET=change-me-16char

# Optional: Claude API Key (if using Claude integration)
ANTHROPIC_API_KEY=sk-ant-api03-...
